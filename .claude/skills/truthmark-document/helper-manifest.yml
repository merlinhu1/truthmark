# Generated by Truthmark 1.4.0. Rerun truthmark init after upgrades.
helpers:
  validate-document-report:
    optional: true
    runner: node>=20
    command: node scripts/validate-document-report.mjs <report-file>
    inputs:
      - document report file
    output: json
    writes: false
    fallback: manually validate support/report-template.md required sections and structured Evidence checked entries
  validate-write-lease:
    optional: true
    runner: node>=20
    command: node scripts/validate-write-lease.mjs <lease-or-report-file> <changed-files-file>
    inputs:
      - lease or worker report yaml
      - changed file list
    output: json
    writes: false
    fallback: manually compare declared allowedWrites and forbiddenWrites with the actual changed files
