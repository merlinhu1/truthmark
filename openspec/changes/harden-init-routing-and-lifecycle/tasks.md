## 0. Baseline and Safety

- [x] 0.1 Record `git status --short --branch` and the protected deletion hash for `tests/package-files.test.ts` (`4d64ae06878c59d1a3979755f99f948e252bbc68b8752fa8f458c2649a90c0ef`); do not restore, stage, edit, or include that deletion in this change.
- [x] 0.2 Run the focused baseline suites for config/init, Check/RepoIndex/scorecard, generated surfaces, filesystem paths, and CLI help so pre-existing failures are separated from implementation regressions.

## 1. Platform-Derived Instruction Routing

- [x] 1.1 Add RED config tests proving existing `instruction_targets` values, including arbitrary source paths, still parse but produce a compatibility diagnostic and never appear in resolved write destinations; prove newly generated configs omit the field.
- [x] 1.2 Add RED renderer cases mapping `claude-code` to `CLAUDE.md`, shared-instruction platforms to `AGENTS.md`, preserving existing host-specific canonical instruction surfaces, deduplicating shared targets, and producing no generic instruction file when no platform is selected.
- [x] 1.3 Add a RED Init integration case with `instruction_targets: ["src/session.ts"]`; prove the source remains byte-for-byte unchanged while only platform-derived instruction surfaces are written.
- [x] 1.4 Keep `instruction_targets` in the raw version-2 schema for compatibility, remove it from resolved write routing and newly generated defaults, and emit actionable ignored-field guidance from config loading or command diagnostics.
- [x] 1.5 Move managed instruction destination selection into platform renderer ownership in `src/templates/generated-surfaces.ts`; remove `config.instructionTargets` from `renderGeneratedSurfaces` and exact-path deduplicate shared platform claims.
- [x] 1.6 Reuse the existing marker-delimited block updater for derived instruction files and rerun config, renderer, and Init tests to GREEN, including preservation of user content outside markers.

## 2. Repository-Wide Routing Coverage

- [x] 2.1 Add a RED fixture to `tests/checks/check.test.ts` with unmapped `backend/auth/session.ts`; assert the exact existing-shape coverage diagnostic, `unmappedSurfaceCount`, and `warn` states for routing coverage and ownership clarity.
- [x] 2.2 Add a RED mapped-unknown-root fixture to `tests/checks/check.test.ts` routing `engine/runtime/**`; assert no coverage diagnostic for the mapped source.
- [x] 2.3 Add `tests/git/files.test.ts` with RED cases for tracked files, untracked visible files, Git-ignored untracked files, configured ignores, deleted tracked files, NUL-delimited filenames containing tabs/newlines/leading or trailing spaces, full-tree fallback's conservative `.gitignore` limitation, path normalization, sorting, and deduplication.
- [x] 2.4 Add mixed-surface RED coverage cases to `tests/checks/check.test.ts` containing ordinary source, Markdown, images, Truthmark-generated files, `tests/**`, nested `*.test.*`, and `*.spec.*`; prove only ordinary functional source receives coverage diagnostics.
- [x] 2.5 Extract `discoverRepositoryFilePaths(rootDir, ignorePatterns)` into new `src/git/files.ts` from the current discovery logic in `src/repo-index/file-tree.ts`; make RepoIndex consume it and keep `tests/repo-index/build.test.ts` output stable.
- [x] 2.6 Export the existing test-path predicate from `src/sync/classify.ts` without adding a new path-classification enum state; rerun `tests/sync/surfaces.test.ts` and `tests/impact/build.test.ts` to prove Sync and Impact behavior is unchanged.
- [x] 2.7 Remove `COVERAGE_SCAN_PATTERNS` from `src/checks/areas.ts`, discover all repository paths through `src/git/files.ts`, and filter to contained, existing, non-test functional code; rerun Check, scorecard, RepoIndex, Impact, and Sync focused suites to GREEN.

## 3. Generated-Surface Ownership and Reconciliation

- [x] 3.1 Extend `tests/templates/generated-surfaces.test.ts` with a RED all-platform/Portal matrix proving every renderer output has exact path, whole-file or managed-block kind, and aggregated platform claims while configured rendered content remains unchanged; include multiple platforms sharing `AGENTS.md`, Portal enabled with one host removed, and Portal disabled while its host remains.
- [x] 3.2 Implement the shared current-surface catalog in `src/templates/generated-surfaces.ts`, exporting the supported platform tuple from `src/config/schema.ts` if needed; do not add a persistent ownership manifest or change workflow body text.
- [x] 3.3 Add RED cases to `tests/checks/check.test.ts` for disabled current platform files, disabled Portal host surfaces, shared desired managed targets, unrelated sibling host files, diverged generated files, and Gemini manual-cleanup diagnostics.
- [x] 3.4 Refactor `src/checks/generated-surfaces.ts` to combine the renderer-derived current catalog with the historical retired registry and produce deterministic inactive/preserve/manual-review entries; rerun Check tests to GREEN.
- [x] 3.5 Add RED exact-file removal and symlink-containment cases to `tests/fs/paths.test.ts`, then implement containment-checked file removal and immediate pre-mutation revalidation in `src/fs/paths.ts`.
- [x] 3.6 Add RED managed-block planning cases to `tests/init/init-instructions.test.ts` for surrounding user content, block-only files, CRLF, shared ownership, duplicate markers, nested markers, reversed markers, and unmatched markers in both desired-update and inactive-removal paths; malformed desired targets must block every Init write.
- [x] 3.7 Implement one structural marker parser for desired upsert, inactive removal, Check, and Uninstall, plus a pure lifecycle plan that classifies `remove-file`, `remove-managed-block`, `preserve`, and `manual-review`; preflight the complete plan before mutation and preserve diverged whole files and all Gemini paths.
- [x] 3.8 Add RED integration cases to `tests/integration/init-check-workflow.test.ts` for two-platform-to-one-platform reconciliation, Portal disablement, sibling preservation, diverged file preservation, removal diagnostics, and idempotent repeated Init.
- [x] 3.9 Apply the shared lifecycle plan from `src/init/init.ts` after desired surfaces are written; remove only safely recognized exact whole files or valid blocks, never host directories, and rerun reconciliation tests to GREEN.

## 4. Safe Uninstall CLI

- [x] 4.1 Add `tests/init/uninstall.test.ts` with RED dry-run/apply parity cases covering `truthmark-lifecycle/v0`, sorted entries, `mode`, `applicable`, `applied`, all six platforms, Portal host surfaces, edited generated files, authored truth/config/templates, Portal presentation output, Gemini, sibling host files, malformed markers, symlinks, hard-linked whole files and managed blocks, missing/invalid config, subdirectory invocation, and idempotence.
- [x] 4.2 Implement `src/init/uninstall.ts` by requesting an empty desired host-surface set from the shared lifecycle planner; preserve config, routes, truth, templates, Portal presentation output, Gemini, and unrelated files.
- [x] 4.3 Add RED parser/help tests to `tests/cli/program.test.ts` and `tests/cli/help.test.ts` for `uninstall --dry-run|--apply`, mutual exclusion, missing mode, `--json`, and concise preservation wording; assert invalid mode selection fails before planning and returns no lifecycle plan, nonzero/no writes for invalid config, malformed markers, or unsafe paths, and successful apply with review diagnostics when diverged files are preserved while other safe entries are removed.
- [x] 4.4 Wire Uninstall through `src/cli/handlers.ts` and `src/cli/program.ts`; require exactly one mode and expose `data.lifecyclePlan` with schema version, mode, sorted exact-path entries, applicability, and applied state through human/JSON output contracts.
- [x] 4.5 Extend `tests/cli/build-artifact.test.ts` with a built-CLI black-box smoke for help, dry-run no-mutation, and apply preservation; run `npm run build` before the test so it cannot pass against stale `dist/main.js`.

## 5. Canonical Truth, User Guidance, and Release Record

- [x] 5.1 Update `docs/truthmark/engineering/contracts/config-route-and-check-contracts.md`, `docs/truthmark/engineering/contracts/generated-host-surfaces.md`, and `docs/truthmark/engineering/behaviors/init-and-scaffold.md` with platform-derived instruction routing, ignored legacy `instruction_targets`, the shared lifecycle plan, platform reconciliation, and uninstall preservation contract.
- [x] 5.2 Update `docs/truthmark/product/capabilities/lane-separated-truth.md`, `docs/truthmark/engineering/behaviors/check-diagnostics.md`, and `docs/truthmark/engineering/repository/repository-intelligence.md` with Git-visible discovery, unknown-root coverage, ignore/test boundaries, scorecard effects, NUL-delimited filenames, and deterministic fallback behavior.
- [x] 5.3 Update `docs/truthmark/engineering/architecture/overview.md` and `docs/truthmark/product/capabilities/agent-native-workflow-injection.md` with renderer-derived ownership, disabled-platform cleanup, and the preserved repository-file runtime model.
- [x] 5.4 Reconcile the complete public CLI contract in `docs/ai/repo-rules.md` with implementation: list `config`, `init`, `check`, `index`, `impact`, `workflow status`, `validate`, and `uninstall`; remove stale `context` and `workflow instructions` claims. Document platform removal, platform-derived instruction placement, the ignored legacy field, dry-run/apply uninstall, preserved files, manual Gemini cleanup, and separate global npm removal in `docs/user-guide.md`.
- [x] 5.5 Add a concise uninstall pointer to `README.md`, update every explicit English CLI command inventory and project-status command list, and apply equivalent storefront guidance to every file under `docs/readmes/README.*.md` as required for a material public command change.
- [x] 5.6 Inspect the complete pending release payload, select the version using normal repository release policy rather than forcing a major for this fix, update `package.json` and root `package-lock.json` together only if that policy requires a bump, and create the versioned change note with platform-derived instruction routing, compatibility behavior, routing discovery, lifecycle reconciliation, and uninstall.
- [x] 5.7 Review `docs/truthmark/routes/areas.md` and the existing check/routing, repository-intelligence, init/scaffold, contracts/commands, and installed-workflow route owners; update routing only if implementation introduces a source path not already covered.

## 6. Verification and Completion

- [x] 6.1 Run focused tests: `node --import tsx --test --test-concurrency=1 tests/config/load.test.ts tests/init/init-instructions.test.ts tests/fs/paths.test.ts tests/git/files.test.ts tests/checks/check.test.ts tests/checks/scorecard.test.ts tests/repo-index/build.test.ts tests/impact/build.test.ts tests/sync/surfaces.test.ts tests/templates/generated-surfaces.test.ts tests/init/uninstall.test.ts tests/cli/program.test.ts tests/cli/help.test.ts tests/integration/init-check-workflow.test.ts` and require zero failures.
- [x] 6.2 Run `npm run typecheck`, `npm run lint`, `npm run format:check`, and `npm run build`; run the built-CLI Uninstall smoke from a temporary Git repository and verify dry-run leaves Git status unchanged while apply preserves authored truth and user files.
- [x] 6.3 Before the broad suite, ensure new regression tests are tracked in the implementation branch or otherwise protected from template/package smoke cleanup; do not stage, restore, or alter the pre-existing deletion of `tests/package-files.test.ts`.
- [x] 6.4 Run `npm run check` and `npm run release:check`; mark change not release-ready because `format:check` blocks due unrelated deleted `tests/package-files.test.ts` in `npm run release:check`.
- [x] 6.5 Run `/opt/data/node/bin/openspec validate harden-init-routing-and-lifecycle --strict`, `/opt/data/node/bin/openspec status --change harden-init-routing-and-lifecycle --json`, `npm run dev -- check --json`, `git diff --check`, and `git status --short --branch`; require the OpenSpec change valid, Truthmark diagnostics explained, no accidental deletions, and only request-traceable files changed.
