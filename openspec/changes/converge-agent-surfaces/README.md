# Converge Agent Surfaces

Converge generated Truthmark workflow surfaces onto source-rendered host-native skill packages. The change preserves native skill resource packaging, keeps non-skill adapters compact, and avoids emitting an unused `.truthmark/agent/` workflow copy when no host surface consumes it.
