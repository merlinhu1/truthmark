# Widget Cache Fixture

This fixture models a small TypeScript service with product truth, engineering behavior truth, API contract truth, operations truth, route ownership, and tests.

The scenario deliberately includes nearby but non-target documents so workflow agents must choose the right lane and owner instead of editing every plausible file.
